<div class="container">
  <div class="row page-title">

    <h1>Request informations</h1>
    <p>latitude:
    <%= @request.latitude %>
    </p>
    <p>longitude:
    <%= @request.longitude %>
    </p>

    <p>request data in coordinates:
    <%= @startcoordinates %>
    </p>

    <p>distance selectionnée:
    <%= @request.distance %> km</p>

    <p>Hotspots correspondant aux categories cochés par le user</p>


    <%# on réhydrate nos hotspot ici %>
      <%# à venir %>

  </div>


  <div class="row simple-form">
    <%# <%= simple_form_for @itinerary do |f| %>

    <%= simple_form_for @itinerary do |f| %>
      <%= f.input :length,
                  as: :hidden,
                  input_html: {value: @request.distance}%>

      <%= f.input :request_id,
                  as: :hidden,
                  input_html: {value: @request.id}%>

      <%# <%= f.input :latitude,
                  # as: :hidden,
                  value: @request.latitude%>

      <%# <%= f.input :longitude,
                  # as: :hidden,
                  value: @request.longitude%>

      <%= f.input :selected_hotspot_ids,
                  as: :hidden,
                  input_html: {value: @selected_hotspot_ids }%>

      <%= f.button :submit,
                    class: "btn-primary"%>

    <% end %>


  </div>

</div>
